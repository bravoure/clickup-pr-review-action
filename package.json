{
  "name": "clickup-pr-review-action",
  "version": "1.0.0",
  "description": "GitHub Action to update ClickUp tasks when PRs are reviewed",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build index.js -o dist"
  },
  "keywords": [
    "github",
    "action",
    "clickup",
    "pull-request",
    "review"
  ],
  "author": "Bravoure",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
